<% provide :page_title, @page.title %>

<h1><%= @page.title %></h1>
<p><%= @page.body %></p>


  <p class="edit-button"><%= link_to 'Редактировать', edit_page_path(params[:path], params[:id]), class: 'btn btn-primary btn-sm' %></p>
  <%= button_to 'Удалить', page_path(params[:path], params[:id]), method: :delete, turbo_confirm: "Вы уверены?", class: 'btn btn-danger btn-sm' %>

<br>

<h3>Дерево подстраниц</h3>
<%#= render 'shared/pages_tree', object: @page.children if @page.children.present? %>

<br>
<br>
<%= render_page_hierarchy(@page.children, "#{@page.name}/") if @page.children.present? %>